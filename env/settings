---
 # If no output is detected from ansible in this number of seconds the execution will be terminated.
idle_timeout: 600
# The maximum amount of time to allow the job to run for (in seconds), exceeding this and the execution will be terminated.
job_timeout: 600 
# Number of seconds for the internal pexpect command to wait to block on input before continuuing
pexpect_timeout: 10
# Use poll() function for communication with child processes instead of select(). select() is used when
# the value is set to ``False``. select() has a known limitation of using only up to 1024 file descriptors.
pexpect_use_poll: True 
# Allow output from ansible to not be printed to the screen
suppress_ansible_output: False 